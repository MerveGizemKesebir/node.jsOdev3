const circleArea = radius => {

    return console.log(Math.PI*(radius * radius));
}

const circleCircumference  = radius => {

    return console.log(Math.PI*(2 * radius));

}


module.exports = {

    circleArea,
    circleCircumference
}
